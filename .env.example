# Environment variables for local development
# Copy this file to .env and adjust as needed

# Server configuration
SERVER_ADDRESS=:8080

# Database configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=api_db

# Alternative: Use DATABASE_URL directly (overrides individual DB_* variables)
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/api_db?sslmode=disable
